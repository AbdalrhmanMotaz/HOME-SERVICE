import 'package:flutter/material.dart';

class NumberOfRequests extends StatelessWidget {
  const NumberOfRequests({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}